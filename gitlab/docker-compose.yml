name: Hello

services:
  gitlab:
    image: gitlab/gitlab-ce:latest
    container_name: gitlab
    restart: always
    hostname: 'gitlab.thethalium.ch'
    ports:
      - '4080:80'
      - '4443:443'
      - '422:22'
    volumes:
      - '$GITLAB_HOME:/config/etc/gitlab'
      - '$GITLAB_HOME:/var/log/gitlab'
      - '$GITLAB_HOME:/var/opt/gitlab'
    shm_size: '256m'
networks:
  default:
    external: true

